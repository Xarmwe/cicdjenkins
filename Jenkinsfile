pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Ajoute des étapes de build ici, par exemple : sh 'make'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Ajoute des étapes de test ici, par exemple : sh 'make test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Ajoute des étapes de déploiement ici
            }
        }
    }
}
